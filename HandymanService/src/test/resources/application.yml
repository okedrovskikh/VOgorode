spring:
  jpa:
    show-sql: true
    open-in-view: false
  liquibase:
    enabled: true
    contexts: test
    change-log: classpath:db/changelog/db.changelog-test.yaml

rest:
  landscape:
    url: ${LANDSCAPE_URL:http://localhost:8082}
